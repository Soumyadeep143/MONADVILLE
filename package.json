{
  "name": "econforge",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/shared && npm run build -w apps/server && npm run build -w apps/web",
    "dev:server": "npm run dev -w apps/server",
    "dev:web": "npm run dev -w apps/web",
    "simulate": "npm run simulate -w apps/server",
    "experiment": "npm run experiment -w apps/server",
    "test": "npm run test -w apps/server",
    "typecheck": "npm run typecheck -w packages/shared && npm run typecheck -w apps/server && npm run typecheck -w apps/web"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
